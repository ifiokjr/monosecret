name: monosecret
description: Native Dart SDK for resolving Monosecret secrets without a separate CLI.
version: 0.3.5
repository: https://github.com/ifiokjr/monosecret/tree/main/dart/monosecret
resolution: workspace
platforms:
  linux:
  macos:
  windows:
environment:
  sdk: ^3.10.0
dependencies:
  code_assets: ^2.0.0
  crypto: ^3.0.7
  ffi: ^2.2.0
  hooks: ^2.0.2
  meta: ^1.16.0
dev_dependencies:
  coverage: ^1.9.0
  test: ">=1.25.0 <1.32.0" # test 1.32+ requires SDK 3.11; CI pins Dart 3.10.9
