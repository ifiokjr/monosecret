---
title: Provider credentials
description: Semantic provider credential names and their environment fallbacks
---

import ProviderCredentials from '../../../components/ProviderCredentials.astro';

Provider credentials let one provider load the authentication material it needs
from another Monosecret provider. They are supported in Monosecret 0.2 and
later.

The table below is the exhaustive reference for accepted semantic credential
names for providers compiled into Monosecret. An explicitly configured provider credential takes precedence over its
environment fallback. When more than one fallback is listed, Monosecret checks
them from left to right. A configured credential that resolves to an empty
value is an error; Monosecret never substitutes the environment fallback for it
(0.4.0+).

External providers negotiate their URI-specific semantic names through the
[Secret Provider Protocol](/reference/provider-protocol) in Monosecret 0.4.0+.
They do not appear in this compile-time catalog. Their alias may omit
`credentials`; Monosecret then uses its provider-private operating-system
keyring namespace. A configured mapping is a lazy source override for the name
the endpoint requests. See the [IPC architecture](/reference/ipc-architecture)
for the boundary and [IPC wire format](/reference/ipc-wire) for its messages.

<ProviderCredentials />

See [Provider credentials](/concepts/providers/#provider-credentials) for
credential source addresses, storage commands, one-hop chaining, and runtime
handling rules.
