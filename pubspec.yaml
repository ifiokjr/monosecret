name: monosecret_workspace
publish_to: none

environment:
  sdk: ^3.10.0

hooks:
  user_defines:
    monosecret:
      native_library_directory: target/debug

workspace:
  - dart/monosecret
  - dart/monosecret_builder

dev_dependencies:
  coverage: ^1.9.0
  melos: ^7.8.1
  test: ^1.25.0
