{
  "name": "monosecret-workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "devenv shell build:all",
    "build:rust": "devenv shell build:dist",
    "check": "devenv shell lint:all",
    "fmt": "devenv shell fix:format",
    "test": "devenv shell test:all",
    "lint:all": "devenv shell lint:all",
    "fix:all": "devenv shell fix:all",
    "coverage": "devenv shell coverage:all",
    "package:check": "devenv shell package:check",
    "release:plan": "monochange plan",
    "release:version": "monochange version",
    "release:publish": "monochange publish"
  },
  "devDependencies": {
    "dprint": "^0.50.0"
  }
}
