#!/usr/bin/env php
<?php

// Thin entry point for the `libmonosecret_ffi` library installer, exposed as
// `vendor/bin/monosecret-install-lib`. The logic lives in scripts/ so it can
// also be run directly with `php scripts/install-ffi-lib.php` from a checkout.
require __DIR__ . '/../scripts/install-ffi-lib.php';
