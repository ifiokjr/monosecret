/vendor/
.phpunit.result.cache
/.phpunit.cache/
# Native artifacts staged for distribution, never committed (see RELEASE.md):
# the bundled monosecret-ffi cdylib and the staged monosecret.so extension.
/lib/
